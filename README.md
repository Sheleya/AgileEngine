# AgileEngine

To run project on your local machine please follow that instructions:
  1) clone repo
  2) run "npm i"
  3) run "react-native link"
  4) launch project via Xcode/Android Studio or with command line "react-native start && react-native run-ios/run-android"
